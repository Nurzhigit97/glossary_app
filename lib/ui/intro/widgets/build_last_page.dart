import 'package:flutter/material.dart';

// В последнем странице есть кнопка для входа в HomeScreen
class BuildLastPage extends StatelessWidget {
  const BuildLastPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color.fromARGB(255, 44, 136, 148),
    );
  }
}
